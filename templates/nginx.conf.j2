location /phpmyadmin {
    root /usr/share;
    index index.php;
}

location ~ ^/phpmyadmin/(.*)\.php$ {
    include fastcgi_params;
    fastcgi_param SCRIPT_FILENAME /usr/share/$fastcgi_script_name;
    fastcgi_pass {{ phpmyadmin_php_fpm_socket }};
}
